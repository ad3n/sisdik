<h1>ImbalanPendaftaran creation</h1>

<form action="{{ path('rewardamount_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('rewardamount') }}">
            Back to the list
        </a>
    </li>
</ul>
