{% extends 'FastSisdikBundle::layout.html.twig' %}

{% block page_heading %}{% trans from "headings" %}settings.admissiongroup.creation{% endtrans %}{% endblock %}

{% block content %}
<form action="{{ path('settings_admissiongroup_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
    {{ form_widget(form) }}
	<div class="form-actions">
	    <button type="submit" class="btn primary">{{ 'label.submit.add'|trans }}</button>
    </div>
</form>
<ul class="pills">
    <li>
        <a href="{{ path('settings_admissiongroup') }}">{{ 'link.backtolist'|trans }}</a>
    </li>
</ul>
{% endblock content %}