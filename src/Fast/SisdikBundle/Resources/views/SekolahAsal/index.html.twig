{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SekolahAsal list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nama</th>
                <th>Kode</th>
                <th>Alamat</th>
                <th>Penghubung</th>
                <th>Ponselpenghubung</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('sekolahasal_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nama }}</td>
                <td>{{ entity.kode }}</td>
                <td>{{ entity.alamat }}</td>
                <td>{{ entity.penghubung }}</td>
                <td>{{ entity.ponselPenghubung }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('sekolahasal_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('sekolahasal_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('sekolahasal_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
