<h1>JenisImbalan list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nama</th>
            <th>Kode</th>
            <th>Keterangan</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('rewardtype_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.nama }}</td>
            <td>{{ entity.kode }}</td>
            <td>{{ entity.keterangan }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('rewardtype_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('rewardtype_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('rewardtype_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
